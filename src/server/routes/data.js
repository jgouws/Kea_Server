function parseQuery(queryRecieved, attributeSelection) {
  var data = [0][0];

  var rowLength;
  var numberRows;

  var entry;

  //Split according to line structure
  //assign rowLength and numberRows
  //load strings into data

  for (var y = 0; y < data.length; y++) {
    for (var x = 0; x < data[0].length; x++) {
      entry = data[y][x];
    }
  }

  return data;
}

//Get bool array
//

function tempClick() {
  console.console.log('onclick working');
}
